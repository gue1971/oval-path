# AGENTS.md

## Git運用ルール（固定）

- 変更が発生したら、作業単位ごとに `git add` と `git commit` を即時実行する。
- `git push` は、ユーザーが明示的に「pushして」と指示した場合のみ実行する。
- コミットメッセージは `feat: ...` / `fix: ...` / `chore: ...` 形式を使い、日本語で要約する。
- 既存のユーザー変更を無断で巻き戻さない（`git reset --hard` などの破壊的操作は禁止）。
